Первоочередные задачи с !!!
<remove name="RoleManager" /> - web.config - fix db 26 error


14/03 

Добавить вывод "Access denied" авторизованому юзеру вместо предложения перелогиниться, добавить норм редирект на ошибку

15/03

Доделать блокировку юзера!!!
UsersManager usersManager = new UsersManager();
                    string currentUserId = User.Identity.GetUserId();
                    bool userIsBlocked = usersManager.GetSpecificUser(currentUserId).UserIsBlocked;

                    if (userIsBlocked)
                    {
                        AuthenticationManager.SignOut(DefaultAuthenticationTypes.ApplicationCookie);
                        return View("AccountBanned");
                    }


17/03 


Добавить ссылки "Back" на все страницы редактирования и на страницу управления оценками для лектора

Отредачить стили записи об отсутствии данных для вывода

Попытаться оставить активно выделенной ссылку с нажатым фильтром


Поменять и типизировать View -> Manage-Index

Сделать нормальные ссылки

Доделать главную страницу

Изменить стиль меню Админа


